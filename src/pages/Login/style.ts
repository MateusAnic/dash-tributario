import styled from "styled-components";
import Box from "@mui/material/Box";

export const ContainerLoginSignPage = styled(Box)`
  display: flex;
  justify-content: center;
  flex-direction: column;
  aling-items: center;
  min-height: 100vh;
  padding: 45px;
`;
