import { ConfirmRegisterSlice, messageRegisterSlice } from "./slice";

export const { setModalVisible, setLoadingVisible } = ConfirmRegisterSlice.actions;

export const { setMessageRegister } = messageRegisterSlice.actions;
